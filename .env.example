# 复制为 .env 或在启动脚本 / K8s / Docker 中注入同名环境变量（勿将含真实路径的 .env 提交入库）
#
# ---------------------------------------------------------------------------
# 必填（生产）：MCP 数据根目录（绝对路径）。决定日志目录与 PDF 导出目录。
#   日志：{WISELINK_MCP_WORKSPACE_ROOT}/logs
#   PDF： {WISELINK_MCP_WORKSPACE_ROOT}/static/exports
# ---------------------------------------------------------------------------
WISELINK_MCP_WORKSPACE_ROOT=D:/path/to/mcp-workspace

# 对外下载链接中的「协议+主机+端口」部分，需与网关/反代一致；与 application.yml 中 app.export.base-url 对应
WISELINK_EXPORT_BASE_URL=http://localhost:8082

# 可选：中文字体路径（TTC 示例带索引），见 WiseLinkExportService
# WISELINK_PDF_FONT=C:/Windows/Fonts/msyh.ttc,0
